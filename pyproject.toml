[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "secretsweep"
version = "0.1.0"
description = "Continuous scanner for secrets and credentials across codebases and infrastructure"
requires-python = ">=3.10"