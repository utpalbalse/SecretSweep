# Pattern matching for detecting secrets
regex>=2023.0.0

# Git repository interaction for history scanning
gitpython>=3.1.40

# YAML parsing for config files and Kubernetes secret scanning
pyyaml>=6.0

# Rich terminal output
rich>=13.0.0

# Testing framework
pytest>=8.0.0
